<%= form_with(model: retention_email) do |form| %>
  <% if retention_email.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(retention_email.errors.count, "error") %> prohibited this retention_email from being saved:</h2>

      <ul>
        <% retention_email.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :date_from, style: "display: block" %>
    <%= form.date_field :date_from %>
  </div>

  <div>
    <%= form.label :date_to, style: "display: block" %>
    <%= form.date_field :date_to %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
